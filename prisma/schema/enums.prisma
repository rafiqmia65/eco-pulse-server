enum Role {
    MEMBER
    ADMIN
}

enum UserStatus {
    ACTIVE
    BLOCKED
    DELETED
}

enum IdeaStatus {
    DRAFT
    REVIEW
    APPROVED
    REJECTED
}

enum PaymentGateway {
    STRIPE
    SSLCOMMERZ
}

enum PaymentStatus {
    PENDING
    PAID
    FAILED
}

enum AIInteractionType {
    CHAT
    CONTENT_GEN
    RECOMMENDATION
    DATA_ANALYSIS
    MODERATION
    PREDICTION
}

enum MessageRole {
    USER
    ASSISTANT
}
